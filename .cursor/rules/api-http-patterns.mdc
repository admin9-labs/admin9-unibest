# API 和 HTTP 请求规范

## HTTP 请求封装
- 项目只使用一套会员 API 请求链，不新增并行请求库
- 页面和 Store 调用 [src/api/member.ts](mdc:src/api/member.ts)
- OpenAPI 生成代码位于 [src/service/](mdc:src/service/)，不得手工编辑
- 生成代码通过 [src/http/openapi-request.ts](mdc:src/http/openapi-request.ts) 接入 [src/http/http.ts](mdc:src/http/http.ts)
- `http.ts` 统一处理 Bearer token、401 刷新重放和错误提示

## API 接口规范
- API 接口定义在 [src/api/](mdc:src/api/) 目录下
- 按功能模块组织 API 文件
- 使用 TypeScript 定义请求和响应类型
- `src/api` 作为稳定业务适配层，不让页面直接依赖生成代码
- 使用 `pnpm openapi` 重新生成接口，使用 `pnpm openapi:check` 检查漂移

## 错误处理
- 网络、HTTP、业务和认证错误统一由 `src/http/http.ts` 转换
- 需要调用方自行提示时使用 `hideErrorToast`
- 受保护请求遇到 401 时自动单飞刷新并最多重放一次
---
globs: src/api/*.ts,src/http/*.ts
---
